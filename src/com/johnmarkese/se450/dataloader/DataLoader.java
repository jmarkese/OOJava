package com.johnmarkese.se450.dataloader;

public interface DataLoader {
	// This was supposed to be a strategy interface but I couldn't implement my design.
	// My implementations had to take unique parameters, so this got demoted to a label
	// public Object load () throws DataLoaderException;
}
