package com.johnmarkese.se450.networksvc.shortestpath;

public interface Vertexable extends Vertex{
	// this was just an idea that I did not implement but I want to keep it around
	// as a reminder, in case it's useful at some point.
	public String vertexName();
}
